/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./*.{html,js}"],
  theme: {
    extend: {},
  },
  plugins: [],
};

//i don't really need the config
//just for tailiwindcss extension to work in VSCode